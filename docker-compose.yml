version: "3.9"

services:
        
  parser:
    build:
      context: ./cdiscount/
    ports:
      - "5000:5000"
    depends_on:
      - splash 

  splash:
    image: scrapinghub/splash
    ports:
    - "8055:8050"

